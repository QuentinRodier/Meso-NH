#!/bin/ksh
#set -x
NARGS=$#
let "N_1=${NARGS} - 1"
export N_1
CMD=`echo $* | cut -d" " -f 1-${N_1} 2>/dev/null `
EXE=`echo $* | cut -d" " -f ${NARGS} `
echo poe `which ${EXE}` ${CMD}
poe `which ${EXE}` ${CMD}
#echo CMD=$CMD
#echo EXE=$EXE
